# macOS entry point; the shared file is also mounted into Agent Toolbox.
set-option -g default-terminal tmux-256color
set-option -as terminal-features ",xterm-ghostty:RGB"
source-file ~/.config/agent-toolbox/shell/tmux.conf
